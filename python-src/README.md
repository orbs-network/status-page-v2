# Project Name

Returns sum of:
    Current balance of Orbs tokens in Coinbase +
    Current balance of team share in Orbs staking contract


## Requirements

- Python 3.x
- Dependencies listed in `requirements.txt`

## Enter virtual enviroment
python -m venv venv
source venv/bin/activate
# add dependancies
pip install -r requirements.txt


